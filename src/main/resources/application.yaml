server:
  port: 8081

spring:
  application:
    name: pharma-cash-management-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/pharma-cash-keycloak
